<div class="flex justify-center items-center h-[100%] w-full">
  <div class="max-w-lg w-full bg-white shadow-lg rounded-lg p-6">
    <h1 class="text-2xl font-bold mb-6 text-center"><%= action_name.capitalize %> District</h1>
    <%= render 'form', district: @district %>
   
    <div class="mt-4"> <!-- Added margin-top -->
      <%= link_to 'Back to Districts', districts_path, class: 'text-white font-bold py-2 px-4 bg-darkBlue rounded-lg hover:bg-gray-600' %>
    </div>
  </div>
</div>
