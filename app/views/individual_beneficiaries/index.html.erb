<h1 class="text-2xl font-bold mb-4">Individual Beneficiaries</h1>
<table class="min-w-full divide-y divide-gray-200">
  <thead class="bg-gray-50">
    <tr>
      <th>Name</th>
      <th>Age</th>
      <th>Phone Number</th>
      <th>Case Name</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody class="bg-white divide-y divide-gray-200">
    <% @individual_beneficiaries.each do |beneficiary| %>
      <tr>
        <td><%= beneficiary.name %></td>
        <td><%= beneficiary.age %></td>
        <td><%= beneficiary.phone_number %></td>
        <td><%= beneficiary.case_name %></td>
        <td>
          <%= link_to 'Show', individual_beneficiary_path(beneficiary), class: 'text-indigo-600 hover:text-indigo-900' %>
          <%= link_to 'Edit', edit_request_individual_beneficiary_path(beneficiary.request, beneficiary), class: 'text-blue-600 hover:text-blue-900' %>
          <%= link_to 'Destroy', individual_beneficiary_path(beneficiary), method: :delete, data: { confirm: 'Are you sure?' }, class: 'text-red-600 hover:text-red-900' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Dashboard', users_path, class: "mt-4 inline-block bg-gray-500 text-white py-2 px-4 rounded hover:bg-gray-700" %>
