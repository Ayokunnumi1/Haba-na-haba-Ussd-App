<!DOCTYPE html>
<html>
  <head>
    <title>HabaNaHaba Ussd App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
    <link rel="manifest" href="/manifest.json" />
  </head>
  <body>
    <main class="bg-gray-50 min-h-screen">
      <% if user_signed_in? %>
        <%= render 'admin_panel/sidebar' %>
        <div class="mt-16 lg:ml-64">
          <% if flash.any? %>
            <div class="text-center mb-2 p-2">
              <% flash.each do |type, message| %>
                <div class="p-4 mb-4 text-sm font-medium rounded-lg font-Outfit
                  <%= 'bg-green-100 text-green-800' if type == 'notice' %>
                  <%= 'bg-red-100 text-red-800' if type == 'alert' %>
                  <%= 'bg-yellow-100 text-yellow-800' if type == 'warning' %>
              ">
                  <%= message %>
                </div>
              <% end %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      <% else %>
        <div>
          <%= yield %>
        </div>
      <% end %>
    </main>
  </body>
</html>