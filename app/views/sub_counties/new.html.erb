<div class="flex justify-center items-center h-full w-full">
  <div class="max-w-lg w-full bg-white shadow-lg rounded-lg p-6">
    <h2 class="text-2xl font-bold mb-6 text-center">
      <%= action_name == 'new' ? 'New SubCounty' : 'Edit SubCounty' %>
    </h2>
    <%= render 'form', sub_county: @sub_county %>

    <div class="mt-4">
      <%= link_to 'Back', sub_counties_path, class: 'bg-darkBlue text-white px-4 py-2 rounded-lg shadow hover:bg-gray-600' %>
    </div>
  </div>
</div>